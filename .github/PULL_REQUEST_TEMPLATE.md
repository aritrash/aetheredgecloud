## Description
What does this PR change? (e.g., "Implemented EL2 to EL1 switch")

## Component Impact
- [ ] **Arch:** `src/arch/aarch64`
- [ ] **Hypervisor:** `src/hypervisor`
- [ ] **Drivers:** `src/drivers`

## Checklist
- [ ] Code compiles with `make build`.
- [ ] Formatted with `cargo fmt`.
- [ ] `unsafe` blocks have `// SAFETY:` comments.
- [ ] Verified on QEMU (Machine: `virt`, CPU: `max`).

## QEMU Serial Output
(Paste a screenshot or text log of the boot/panic here)